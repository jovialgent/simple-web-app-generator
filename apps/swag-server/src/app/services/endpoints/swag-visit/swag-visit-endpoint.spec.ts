import { SwagVisitEndpoint } from './swag-visit-endpoint';

describe('SwagVisitEndpoint', () => {
  it('should create an instance', () => {
    expect(new SwagVisitEndpoint()).toBeTruthy();
  });
});
