import { SwagBasicServerManager } from './swag-basic-server-manager';

describe('SwagBasicServerManager', () => {
  it('should create an instance', () => {
    expect(new SwagBasicServerManager()).toBeTruthy();
  });
});
