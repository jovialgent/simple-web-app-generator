import { SwagBasicServerManagerUtils } from './swag-basic-server-manager-utils';

describe('SwagBasicServerManagerUtils', () => {
  it('should create an instance', () => {
    expect(new SwagBasicServerManagerUtils()).toBeTruthy();
  });
});
