import { SwagBasicServerVisitManager } from './swag-basic-server-visit-manager';

describe('SwagBasicServerVisitManager', () => {
  it('should create an instance', () => {
    expect(new SwagBasicServerVisitManager()).toBeTruthy();
  });
});
