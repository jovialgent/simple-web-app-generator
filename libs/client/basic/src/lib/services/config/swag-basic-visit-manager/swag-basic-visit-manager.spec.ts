import { SwagBasicVisitManager } from './swag-basic-visit-manager';

describe('SwagBasicVisitManager', () => {
  it('should create an visit', () => {
    expect(new SwagBasicVisitManager()).toBeTruthy();
  });
});
