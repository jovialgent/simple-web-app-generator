import { SwagBasicUiClasses } from './swag-basic-ui-classes';

describe('SwagBasicUiClasses', () => {
  it('should create an instance', () => {
    expect(new SwagBasicUiClasses()).toBeTruthy();
  });
});
