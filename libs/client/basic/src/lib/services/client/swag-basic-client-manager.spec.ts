import { SwagBasicClientManager } from './swag-basic-client-manager';

describe('SwagBasicClientManager', () => {
  it('should create an instance', () => {
    expect(new SwagBasicClientManager()).toBeTruthy();
  });
});
